# Node

Node.js — a [[JavaScript]] runtime built on V8. Allows JS to run server-side and in CLI tools.

## Usage in CodeGraphy

The extension host runs in Node via VS Code's extension process.
[[TypeScript]] compiles to CommonJS modules that Node can execute.

## See Also

- [[Tooling]] — npm, package management
- [[Architecture]] — extension host environment
