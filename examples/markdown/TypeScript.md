# TypeScript

A statically typed superset of [[JavaScript]] that compiles to plain JS.

## Features

- Static type checking
- Interfaces and generics
- Decorators

## Ecosystem

- Used heavily in [[React]] projects
- Powers [[Architecture|the extension architecture]] here in CodeGraphy
- See [[Tooling]] for compiler setup (tsc, esbuild, Vite)
