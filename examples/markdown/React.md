# React

A [[JavaScript]] library for building user interfaces using components.

## Language

React is written in [[TypeScript]] (and JS). JSX/TSX syntax compiles to `React.createElement` calls.

## Build

See [[Tooling]] for Vite + React setup.

## In CodeGraphy

The webview panel is a React app. See [[Architecture]] for how the extension and webview communicate.
