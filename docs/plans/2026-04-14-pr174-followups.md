# PR 174 follow-ups

## Scope

- disabled plugin => disable its default filter patterns too
- save reindex => changed file + plugin-requested dependents only, not full repo
- remove legacy `.vscode` / old-shape settings migration paths
- invalid `.codegraphy/settings.json` save => ignore, keep last valid in-memory settings
- same source+target+kind but different relation `type` => keep distinct graph edges
- refine plugin lifecycle through `plugin-api`, centered on incremental `onFilesChanged(...)`

## Plan

1. red tests: plugin filters, invalid settings reload, save-scope reindex, relation-type edge split
2. implement settings cleanup: no legacy migration, invalid-save guard
3. finish incremental lifecycle path in `plugin-api` + registry + built-ins + docs
4. update graph edge identity/build so relation `type` survives to graph/render/export
5. targeted tests, then broader extension test/lint/typecheck sweep

## Unresolved questions

- none blocking; lifecycle refinement assumed to mean shipping/documenting `onFilesChanged(...)`
