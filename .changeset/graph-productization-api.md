---
"@codegraphy-vscode/plugin-api": major
"@codegraphy/extension": major
---

Broaden the plugin graph API with canonical `kind`/`sources` graph contracts, host-backed graph queries, exporter registration, named graph slots, tooltip actions, and scoped view recompute dependencies.
