---
"@codegraphy/mcp": minor
"@codegraphy/core": patch
---

Add path-first CodeGraphy Workspace status, indexing, and MCP query tools backed by `@codegraphy/core` without opening or focusing VS Code. Remove the legacy `codegraphy open` and `codegraphy list` repo-registry workflow so the CLI matches the new workspace model.
