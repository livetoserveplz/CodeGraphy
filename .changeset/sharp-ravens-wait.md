---
"@codegraphy/extension": patch
---

Prevent agent-triggered reindex from crashing or being interrupted by graph reloads while the saved index is temporarily cleared, including preserving analyzer method context during full refresh.
